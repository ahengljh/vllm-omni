# AR To DiT Two-Stage

This directory is reserved for the split `AR -> DiT` Bagel setup.

Planned contents:

- split stage-config yaml
- split serve wrapper
- split-specific probe wrappers if the common bagel probes are not sufficient

Current status:

- `serve_ar_to_dit_two_stage.sh` is prepared
- the actual split yaml is not materialized yet
